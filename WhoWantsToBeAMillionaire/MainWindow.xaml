<Window x:Class="WhoWantsToBeAMillionaire.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WhoWantsToBeAMillionaire"
        mc:Ignorable="d"
        FontFamily="Source Sans Pro"
        Title="MainWindow" Height="630" Width="1140"
        MinWidth="580" MinHeight="500">
    <Window.Background>
        <ImageBrush ImageSource="pack://application:,,,/WhoWantsToBeAMillionaire;component/Resources/Media/bg.jpg" Stretch="UniformToFill"/>
    </Window.Background>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="20*"/>
        </Grid.RowDefinitions>
        
        <!--Meniul de sus-->
        <StackPanel Name="UpperMenu"  Orientation="Horizontal">
            <Button Name="btToggleVolume"
                    Click="btToggleVolume_Click"
                    Content="VOL ON"/>
            <Button Name="btAdminPanel"
                    Click="btAdminPanel_Click"
                    Content="Admin Panel"/>
        </StackPanel>

        <ContentControl Grid.Row="1" Content="{Binding SelectedViewModel, Mode=TwoWay}"/>
    </Grid>
</Window>
