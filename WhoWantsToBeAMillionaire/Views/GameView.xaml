<UserControl x:Class="WhoWantsToBeAMillionaire.Views.GameView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WhoWantsToBeAMillionaire.Views"
             mc:Ignorable="d" Background="White" Height="492" Width="974">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="7*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="2*"/>
                <RowDefinition Height="7*"/>
                <RowDefinition Height="5*"/>
                <RowDefinition Height="3*"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Orientation="Horizontal">
                <Button x:Name="btFiftyFifty" Click="btFiftyFifty_Click"  Content="Lifeline 1" Background="Blue"/>
                <Button x:Name="btAskAudience" Click="btAskAudience_Click" Content="Lifeline 1" Background="Blue"/>
                <Button x:Name="btPhoneCall" Click="btPhoneCall_Click"  Content="Lifeline 1" Background="Blue"/>
            </StackPanel>

            <Label Grid.Row="1" 
                   FontSize="40" 
                   HorizontalAlignment="Center" VerticalAlignment="Center" 
                   Content="{Binding CurrentQuestion.QuestionText}"/>

            <Grid Grid.Row="2">
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>

                <Button Click="btOption_Click" Grid.Row="0" Grid.Column="0" Tag="0" Content="{Binding CurrentQuestion.Options[0]}" FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Center" Background="AliceBlue"/>
                <Button Click="btOption_Click" Grid.Row="0" Grid.Column="1" Tag="1" Content="{Binding CurrentQuestion.Options[1]}" FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Center" Background="AliceBlue"/>
                <Button Click="btOption_Click" Grid.Row="1" Grid.Column="0" Tag="2" Content="{Binding CurrentQuestion.Options[2]}" FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Center" Background="AliceBlue"/>
                <Button Click="btOption_Click" Grid.Row="1" Grid.Column="1" Tag="3" Content="{Binding CurrentQuestion.Options[3]}" FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Center" Background="AliceBlue"/>
            </Grid>

            <Label Grid.Row="3" Name="lbExplications" 
                   Content="{Binding Explanations}" 
                   HorizontalContentAlignment="Center" VerticalContentAlignment="Center" 
                   FontSize="20" 
                   Background="Azure" 
                   Visibility="Collapsed"/>
        </Grid>

        <!--<Label Grid.Column="1" Content="Prize list" Background="Purple"/>-->
        <StackPanel 
            x:Name="prizeStack"
            CanVerticallyScroll="True" 
            Grid.Column="1" 
            Orientation="Vertical" 
            VerticalAlignment="Center" 
            HorizontalAlignment="Center">
        </StackPanel>
    </Grid>
</UserControl>
